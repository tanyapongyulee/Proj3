import static org.junit.Assert.*;

import org.junit.Test;


public class aStarTest {

  @Test
	public void testHeuristic() {
		/*Board c = new Board("tricky.txt");
		c.buildBoard();
		c.placeBlocks();
		c.printBoard();
		aStar a = new aStar();
		Board d = new Board("trickygoal.txt",c.getLength(),c.getWidth());
		a.match(d, c);
		assertTrue(true);
		c.printBoard();
*/
		Board notc = new Board("test1.txt");
		notc.buildBoard();
		notc.placeBlocks();
		notc.printBoard();
		aStar nota = new aStar();
		Board notd = new Board("test1goal.txt",notc.getLength(),notc.getWidth());
		nota.match(notd, notc);
		assertTrue(true);
		notc.printBoard();
		
	}
	

}
